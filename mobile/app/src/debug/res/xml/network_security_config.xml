<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only network security config. Allows plain HTTP (cleartext) to the
  emulator's host-loopback alias and common private LAN ranges so the app
  can talk to a local `docker compose up` backend during development.
  This file lives under src/debug/ so it is never packaged into a release
  build — release traffic stays HTTPS-only via the platform default.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
